services:
    db:
        image: postgres
        restart: always
        env_file:
            - .env
        volumes:
            - ./pgdata:/var/lib/postgresql/data

    adminer:
        image: adminer
        restart: always
        ports:
            - 5432:8080
