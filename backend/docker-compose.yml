services:
    db:
        image: postgres:15
        restart: always
        environment:
            - POSTGRES_PASSWORD=0b3y#Mxtress
        volumes:
            - ./pgdata:/var/lib/postgresql/data

    adminer:
        image: adminer
        restart: always
        ports:
            - 8080:8080
